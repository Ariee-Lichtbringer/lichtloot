/* Focus the requested item; leave character, raid and priority selection to the user. */
(()=>{function apply(){const params=new URLSearchParams(location.search),name=params.get('itemSearch');if(!name)return;const input=document.getElementById('itemSearch');if(!input)return;input.value=name.slice(0,200);input.dispatchEvent(new Event('input',{bubbles:true}));const note=document.createElement('p');note.setAttribute('role','status');note.textContent=`Vorgemerktes Item${params.get('plannedFor')?' für '+params.get('plannedFor').slice(0,120):''}: ${name.slice(0,200)}. Wähle den passenden Charakter und Raidtermin, lege deine Prio fest und speichere sie.`;input.before(note);input.scrollIntoView({block:'center'});input.focus();}if(document.readyState==='complete')apply();else window.addEventListener('load',apply,{once:true});})();
